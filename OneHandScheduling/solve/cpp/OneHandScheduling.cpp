#include <vector>
#include <string>
using namespace std;

class OneHandScheduling {
public:
  string checkSchedule(vector<int> const &tStart,
                       vector<int> const &tEnd) {
    return "$";
  }
};
