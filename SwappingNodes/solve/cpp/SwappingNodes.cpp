#include <vector>
#include <string>
using namespace std;

class SwappingNodes {
public:
  vector<int> swapNodes(vector<int> const &leaves,
                        int numberOfLeaves) {
    return vector<int>();
  }
};
